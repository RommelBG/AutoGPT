# Bot Autonome EU5 — dépendances

# Appels LLM (conseillers via endpoints OpenAI-compatibles : Cerebras / DeepSeek / Groq)
httpx>=0.27,<1.0

# Coordinateur final (Claude Sonnet 4.6) via le SDK officiel Anthropic
anthropic>=0.49

# Chargement de la configuration depuis un fichier .env
python-dotenv>=1.0

# --- Dépendances spécifiques Windows (optionnelles) ---
# Installées uniquement sur la machine de jeu. Le bot fonctionne sans elles
# grâce aux backends "mock" (voir memory/backend.py et actions/backend.py).
#
#   pymem>=1.13         # lecture/écriture mémoire du processus EU5
#   pyautogui>=0.9.54   # contrôle clavier/souris du jeu
#   pygetwindow>=0.0.9  # localisation de la fenêtre du jeu
#
# L'interface desktop utilise tkinter, inclus dans la bibliothèque standard Python.
